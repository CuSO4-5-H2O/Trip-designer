# render-20260713j 发布说明

日期：2026-07-13

## 本次变更

- 在 `render-20260713i` 的云端唯一同步护栏和文档导入基础上，补齐路线段交通修改入口。
- `map-providers-lite.js` 的路线摘要中，如果路线段关联到具体 activity，会显示“修改”按钮。
- 点击路线段“修改”会打开同一事项的交通编辑状态，保存后仍走 `TripPlanner.saveExternalLibrary` 统一同步通道。
- 首页资源版本更新为：

```text
render-20260713j
./map-providers-lite.js?v=render-20260713j
```

## 同步与数据安全

- 不修改 Render Persistent Disk 中已有 `/var/data/rooms.json`。
- 不删除、不重命名、不迁移任何已有行程单。
- 云端房间启动时继续由 `cloud-sync-guard.js` 阻止本地启动缓存或默认空白行程抢占服务器数据。

## Smoke test

- `.github/workflows/smoke-test.yml` 已同步检查 `render-20260713j` 和新的地图脚本版本。
- 保留 workflow concurrency，减少旧 smoke run 失败通知堆叠。

## 主要提交

- `8c1dd1c`：路线段新增交通修改入口。
- `11df3a3`：首页升级到 `render-20260713j` 并加载新地图脚本版本。
- `93045e0`：Smoke test 同步检查 `render-20260713j`。
