# render-20260713k 发布说明

日期：2026-07-13

## 本次热修

- 修复页面可能卡死的问题：旧版 `startup-cache-guard.js` 每次刷新都会用时间戳创建新的 `trip-planner-recovery:*` 本机备份，长期刷新后会产生大量重复大缓存。
- 新版 `startup-cache-guard.js` 改为按内容指纹保存 recovery 备份，同一份本机缓存只备份一次。
- 修复“历史行程恢复入口不出现 / 页面卡住”的问题：`local-recovery.js` 现在会：
  - 优先读取当前浏览器原始本机缓存；
  - 对 recovery 原始内容做去重；
  - 限量解析候选，避免主线程长时间 JSON.parse；
  - 没有本机备份时显示“无本机备份”，避免用户误以为按钮丢失。

## 数据安全

- 不删除 Render 云端数据。
- 不删除浏览器本机已有 recovery 备份。
- 恢复操作仍然只会生成新的 `本机恢复 - ...` list，不覆盖当前服务器 list。

## 首页版本

```text
render-20260713k
./startup-cache-guard.js?v=render-20260713k
./local-recovery.js?v=render-20260713k
```

## 主要提交

- `30e3d4e`：启动本机恢复备份按内容去重。
- `948e2ee`：本机恢复扫描去重、限量解析，并显示空状态。
- `7934b95`：首页升级到 `render-20260713k`。
- `6e2dad1`：Smoke test 同步检查恢复热修版本。
