# render-20260713l 发布说明

日期：2026-07-13

## 本次变更

- 左侧行程单区域的“新建”按钮改为纯 `+`。
- `+` 现在作为统一入口，菜单内集成：

```text
新建空白行程单
从文档导入
快速导入
恢复本机备份
导出当前行程单
```

- 隐藏原本单独显示的“无本机备份 / 恢复本机行程 / 导出 / 文档导入”等分散按钮，减少左侧按钮堆叠。
- 恢复本机备份改为点击菜单后按需扫描当前浏览器缓存，不在页面启动时抢主线程。
- 恢复仍然只会创建新的 `本机恢复 - ...` list，不覆盖云端当前 list。

## 关于“本机备份丢失”

- 如果当前浏览器里的 `localStorage/sessionStorage` 已经被浏览器清理、无痕模式关闭、换设备、或之前 destructive 版本已经删除且没有 recovery key，则网页无法凭空恢复本机数据。
- 本轮没有删除任何本机 recovery key，也不会删除 Render 云端数据。
- 菜单里的“恢复本机备份”会同时检查：
  - 当前房间原始本机缓存；
  - 启动保护保存的 session 备份；
  - `trip-planner-recovery:${room}:*` recovery 备份。

## 首页版本

```text
render-20260713l
./list-plus-menu.js?v=render-20260713l
```

## 主要提交

- `f464c3f`：新增统一 list `+` 菜单。
- `b80ff09`：首页加载 `list-plus-menu.js` 并升级到 `render-20260713l`。
- `2efae62`：Smoke test 检查统一菜单脚本。
