# render-20260713r 空白区域添加日期修复

## 修复内容

- 将行程列表底部的虚线 `+` 区域从纯展示元素改为真实按钮。
- 单击虚线区域即可添加日期，不再需要双击或弹出二级菜单。
- 支持键盘 `Enter` / 空格触发添加日期。
- 每次新增日期并触发页面重渲染后，虚线区域会自动重新绑定并继续可用。
- 达到天数上限时显示“已达到天数上限”，不会静默失效。
- 首页资源版本更新为 `render-20260713r`，并加载 `interaction-hotfix.js?v=render-20260713r`。
- Smoke test 同步更新到 `render-20260713r`。

## 数据影响

- 不修改任何已有 room 数据。
- 不修改 GitHub `trip-data/rooms.json` 中已有行程。
- 只改变前端点击行为和资源版本。

## 主要提交

- `194454e`：虚线区域改为可点击按钮并绑定添加日期。
- `57577d0`：页面加载新版交互脚本。
- `844ea07`：Smoke test 更新到 `render-20260713r`。
