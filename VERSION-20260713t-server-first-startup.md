# render-20260713t 云端优先首屏修复

## 修复内容

- 修复进入协作房间时短暂闪现旧本地行程的问题。
- `startup-cache-guard.js` 不再 10 秒后自动放开本地缓存读取。
- 带 `room=` 的协作页面首屏不再自动使用浏览器本地缓存作为数据源。
- 旧本地缓存仍会被保存为 recovery 备份，可通过“恢复本机备份”手动导入。
- 页面版本更新为 `render-20260713t`，并加载 `startup-cache-guard.js?v=render-20260713t`。
- Smoke test 更新到 `render-20260713t`。

## 原因说明

此前页面为了容错，会临时屏蔽本地缓存 10 秒；如果网络或脚本加载稍慢，本地旧缓存可能在云端数据到达前被读取并渲染，造成旧行程短暂闪屏。现在协作房间始终以服务器为首屏数据源。

## 数据影响

- 不修改已有 room 数据。
- 不删除本地旧缓存，只是不再自动拿它做首屏渲染。
- 不修改 GitHub `trip-data/rooms.json` 中已有行程。

## 主要提交

- `08c092a`：协作房间启动时持续屏蔽本地缓存读取。
- `aa69776`：页面加载 `render-20260713t` 资源。
- `5a1cf4b`：Smoke test 更新到 `render-20260713t`。
