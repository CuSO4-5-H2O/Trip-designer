# render-20260713m

## 目标

修复快速规划和日期添加入口重复的问题，让页面只保留一个明确的收起按钮和一个顶部添加日期入口。

## 更改

- 隐藏快速规划区域里由旧轻量脚本额外生成的重复收起/展开按钮，只保留文档导入组件旁边的收起按钮。
- 隐藏每日列表下方重复的“添加日期”按钮，保留顶部状态栏旁的“添加日期”。
- 在每日列表空白处双击时显示固定悬浮菜单，点击后添加新日期。
- 本次更改不迁移、不覆盖、不清理任何已保存行程单数据。

## 验证

- GitHub smoke test 检查 `render-20260713m` 和 `timeline-ui-cleanup.js` 是否已被页面加载。
- 运行时仍通过现有 `/healthz`、地图配置、AI 方法限制和静态资源检查。
